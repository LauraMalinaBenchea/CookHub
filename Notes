When creating a new page for the fronted, make sure to add it to the navbar if it needs to be easily trackable.
Make sure to also register it under App.js, where we must keep track of all pages.
If you don't add it there, you might encounter errors like "No route matched location".


TODO:
add management command to populate available units;
fix units conversion;
create view only page for recipes and redirect there from public recipe view;
make something of the home page;
fix automatic recipe generation - ensure everything is set up model-wise for recipes.
fix profile preferences page
